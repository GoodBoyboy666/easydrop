services:
  app:
    image: ghcr.io/goodboyboy666/easydrop:latest
    pull_policy: always
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data
